////////////////////////////////////////////////////////////////////////////// ADOBE SYSTEMS INCORPORATED// Copyright 2017 Adobe Systems Incorporated// All Rights Reserved//// NOTICE:  Adobe permits you to use, modify, and distribute this file in accordance with the// terms of the Adobe license agreement accompanying it.  If you have received this file from a// source other than Adobe, then your use, modification, or distribution of it requires the prior// written permission of Adobe.//////////////////////////////////////////////////////////////////////////////**   @fileoverview Shows how to use a EditCaptureTime object to update capture time of selected jpeg and camera raw files.  @class Shows how to use a EditCaptureTime object to update capture time of selected jpeg and camera raw files    <h4>Usage</h4>   <ol>   <li>Open the snippet in the ExtendScript Toolkit (see Readme.txt), setting Bridge CC 2018 as the target application.      <li> In Bridge, select the fies which you want to update capture time.   <li> Select one of the three APIs mentioned for Editing Capture Time and remove other two.   <li> Assign parameters value to API according to your use.   <li> Execute the script and your selected files will have the updated capture time.   </ol>   <h4>Description</h4>     <p>Shows how to update capture time from Bridge, using the    EditCaptureTime object and the ExtendScript File object for multiple files.        <p>You must select atleast a single file for the Edit Capture Time to work.   .<br />   @constructor Constructor   */function SnpEditCaptureTime(){	/**	 The context in which this snippet can run.	 @type String	*/	this.requiredContext = "\tExecute against Bridge.\nBridge must be running and \n" 		+ "select images for edit capture time";	$.level = 1; // Debugging level}/** Functional part of this snippet.    Gets a Edit Capture Time object and calls required functions used in editing capture time*/SnpEditCaptureTime.prototype.run = function(){	var retval = true;	if(!this.canRun()) {		retval = false;			return retval;	}    var ect = new EditCaptureTime("Created by SnpEditCaptureTime"); 	// Add selected files on Bridge for Edit Capture Time   for (var i=0; i< app.document.selectionLength; i++)    {        var t = app.document.selections[i];        ect.addFilesForECT(t.path);    }    // Edit Capture Time supports Jpeg and Camera Raw files.    // For Edit Capture Time , we have 3 options:-    // 1)   Set all selected files all to corrected time    // 2)   Shift all files by set number of hours,minutes and seconds    // 3)  Revert capture time to original capture time of selected files ( whose capture time has been updated)        // For setting all files capture time to a corrected time , API is  setToCorrectedTime    var date = 27; // Date has to be valid between 1 to 31 ( according to month)    var month = 10; // Month has to be between 1 to 12    var year = 2018; // Valid year    var hours = 21 ; // between 0 to 24    var minutes = 15; // beween 0 to 60    var seconds = 10; // between 0 to 60    ect.setToCorrectedTime(date,month,year,hours,minutes,seconds);        // For shifting capture time of all files by set number of hours ,minutes and seconds, API is shiftBySetTime    var sign = "+" ; // Time has to be added or subtracted.    var hoursShift = 2; // values to be between 0 to 24    var minutesShift = 10; // values to be between 0 to 60    var secondsShift = 19; // values to be between 0 to 60    // Minimum 2 parameters required by function.     // Commenting this API , because 1 API should be used out of the options in 1 script.    // ect.shiftBySetTime(sign,hoursShift,minutesShift,secondsShift);    // For reverting Capture time of all selected files , API is revertToOriginalTime    //ect.revertToOriginalTime();    	return retval;}/** Determines whether snippet can be run given current context. The snippet fails if these preconditions are not met:  <ul> <li> Must in Bridge <li> A single image file must be selected in Bridge </ul> @return True if this snippet can run, false otherwise @type Boolean*/SnpEditCaptureTime.prototype.canRun = function(){			// must run in Bridge 	// must be single image file	if((BridgeTalk.appName == "bridge") && app.document.selectionLength > 0)	{		return true;	}	// Fail if these preconditions are not met.  	$.writeln("ERROR:: Cannot run SnpEditCaptureTime");	$.writeln(this.requiredContext);	return false;}/** "main program": construct an anonymous instance and run it  as long as we are not unit-testing this snippet.*/if(typeof(SnpEditCaptureTime_unitTest)  == "undefined") {    new SnpEditCaptureTime().run();}